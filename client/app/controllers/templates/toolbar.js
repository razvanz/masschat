( function () {
    'use strict';

    var toolbarCtrl = function(){
      var self = this;

    };

    toolbarCtrl.$inject = [];

    angular.module( 'app' ).controller( 'toolbarCtrl', toolbarCtrl );

} )();
