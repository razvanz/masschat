( function () {
    'use strict';

    var shellCtrl = function($rootScope, $scope, session, $cookies){
      var self = this;

    };

    shellCtrl.$inject = ['$rootScope', '$scope', 'session' , '$cookies'];

    angular.module( 'app' ).controller( 'shellCtrl', shellCtrl );

} )();
